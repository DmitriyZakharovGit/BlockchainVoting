## BlockchainVoting - Space for voting
This project allows voting and storing votes in a decentralized blockchain. The multichain is used as a blockchain.

## 📝 Available Scripts

In the project directory, you can run:

### `npm build`

Builds the app for production to the `public` folder.<br />

## 📝 Server start

Trivial run node:
`node app.js`.

Run node in daemon mode:
`pm2 start app.js`

Monitoring all processes launched:
`pm2 monit`
